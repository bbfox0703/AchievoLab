<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="RunGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="Root">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top CommandBar -->
        <CommandBar Grid.Row="0" x:Name="MainCommandBar">
            <AppBarButton x:Name="RefreshButton" Icon="Refresh" Label="Refresh" Click="OnRefresh" ToolTipService.ToolTip="Refresh"/>
            <AppBarButton x:Name="StoreButton" Icon="Save" Label="Store" Click="OnStore" ToolTipService.ToolTip="Store"/>
            <AppBarSeparator/>
            <AppBarButton x:Name="LockAllButton" Icon="DisableUpdates" Label="Lock All" Click="OnLockAll" ToolTipService.ToolTip="Lock All"/>
            <AppBarButton x:Name="UnlockAllButton" Icon="Pin" Label="Unlock All" Click="OnUnlockAll" ToolTipService.ToolTip="Unlock All"/>
            <AppBarButton x:Name="InvertAllButton" Icon="Switch" Label="Invert All" Click="OnInvertAll" ToolTipService.ToolTip="Invert All"/>
            <AppBarButton x:Name="ResetStatsButton" Icon="Delete" Label="Reset Stats" Click="OnResetAllStats" ToolTipService.ToolTip="Reset Stats"/>
            <AppBarToggleButton x:Name="AutoMouseMoveButton" Icon="Target" Label="Auto Mouse" Checked="OnAutoMouseMove" Unchecked="OnAutoMouseMove" ToolTipService.ToolTip="Auto Mouse"/>
            <AppBarToggleButton x:Name="TimerToggleButton" Icon="Clock" Label="Timer" Click="OnTimerToggle" ToolTipService.ToolTip="Timer"/>
        </CommandBar>

        <!-- Main Content -->
        <TabView Grid.Row="1" x:Name="MainTabView">
            <!-- Achievements Tab -->
            <TabViewItem Header="Achievements">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Achievement Filters -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="10">
                        <TextBox x:Name="SearchTextBox" PlaceholderText="Search achievements..." Width="200" TextChanged="OnSearchTextChanged"/>
                        <ToggleButton x:Name="ShowLockedButton" Content="Show Locked Only" Click="OnShowLockedToggle"/>
                        <ToggleButton x:Name="ShowUnlockedButton" Content="Show Unlocked Only" Click="OnShowUnlockedToggle"/>
                        <ComboBox x:Name="LanguageComboBox" PlaceholderText="Language" Width="120" SelectionChanged="OnLanguageChanged"/>
                        <ComboBox x:Name="ColumnLayoutComboBox" PlaceholderText="Column Layout" Width="150" SelectionChanged="OnColumnLayoutChanged"/>
                        <Button x:Name="SetTimerButton" Content="Set Timer" Click="OnSetTimer"/>
                        <TextBlock Text="⚪ Timer Off" x:Name="TimerStatusText" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Achievement List -->
                    <ListView Grid.Row="1" x:Name="AchievementListView"
                              SelectionMode="Extended"
                              Margin="10,0,10,10">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <ListViewItemPresenter
                                                Content="{TemplateBinding Content}"
                                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                                ContentMargin="{TemplateBinding Padding}"
                                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                                BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                BorderThickness="1"
                                                CornerRadius="8"
                                                SelectedBackground="{ThemeResource SystemControlHighlightListAccentBrush}"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5" Padding="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="220"/>
                                        <ColumnDefinition Width="250"/>
                                        <ColumnDefinition Width="300"/>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Checkbox -->
                                    <CheckBox Grid.Column="0" 
                                              IsChecked="{Binding IsAchieved, Mode=TwoWay}" 
                                              IsEnabled="{Binding IsNotProtected}"
                                              Content="✓"
                                              Margin="0,0,10,0"/>
                                    
                                    <!-- Protection Indicator (only show for protected, unachieved) -->
                                    <TextBlock Grid.Column="0" 
                                               Text="🔒" 
                                               FontSize="12"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Top"
                                               Margin="0,-5,-5,0"
                                               Visibility="{Binding LockVisibility}"/>

                                    <!-- Achievement Icon -->
                                    <Border Grid.Column="1" 
                                            Width="48" Height="48" 
                                            CornerRadius="4"
                                            Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
                                            Margin="0,0,10,0">
                                        <Grid>
                                            <!-- Steam Icon (bound to IconImage property) -->
                                            <Image Source="{Binding IconImage}" 
                                                   Stretch="Uniform"
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"/>
                                            <!-- Fallback emoji (always visible, will be covered by icon when loaded) -->
                                            <TextBlock Text="🏆" 
                                                       FontSize="20" 
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       Opacity="0.7"/>
                                        </Grid>
                                    </Border>

                                    <!-- ID -->
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Id}" 
                                               FontFamily="Consolas" 
                                               FontSize="12"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>

                                    <!-- Name -->
                                    <TextBlock Grid.Column="3" 
                                               Text="{Binding Name}" 
                                               FontWeight="SemiBold" 
                                               TextWrapping="Wrap"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>

                                    <!-- Description -->
                                    <TextBlock Grid.Column="4" 
                                               Text="{Binding Description}" 
                                               TextWrapping="Wrap" 
                                               Opacity="0.8"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>

                                    <!-- Unlock Time -->
                                    <TextBlock Grid.Column="5" 
                                               Text="{Binding UnlockTime}" 
                                               FontFamily="Consolas" 
                                               FontSize="12"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"/>

                                    <!-- Scheduled Time -->
                                    <TextBlock Grid.Column="6"
                                               Text="{Binding ScheduledUnlockTime}"
                                               FontFamily="Consolas"
                                               FontSize="12"
                                               VerticalAlignment="Center"
                                               Foreground="{ThemeResource SystemControlForegroundAccentBrush}"
                                               Margin="0,0,10,0"/>

                                    <!-- Permission -->
                                    <TextBlock Grid.Column="7" 
                                               Text="{Binding Permission}" 
                                               FontFamily="Consolas" 
                                               FontSize="12"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.Header>
                            <Grid Margin="5" Padding="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="300"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Achieved" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="1" Text="Icon" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="2" Text="ID" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="3" Text="Name" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="4" Text="Description" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="5" Text="Unlock Time" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="6" Text="Scheduled Time" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="7" Text="Permission" FontWeight="Bold"/>
                            </Grid>
                        </ListView.Header>
                    </ListView>
                </Grid>
            </TabViewItem>

            <!-- Statistics Tab -->
            <TabViewItem Header="Statistics">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <CheckBox Grid.Row="0" x:Name="EnableStatsEditingCheckBox" 
                              Content="I understand that modifying stats can cause issues"
                              Margin="0,0,0,10"/>

                    <ListView Grid.Row="1" x:Name="StatisticsListView" IsEnabled="{Binding IsChecked, ElementName=EnableStatsEditingCheckBox}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5" Padding="10" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" 
                                      CornerRadius="8" BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="250"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Id}" FontFamily="Consolas" FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBox Grid.Column="2" Text="{Binding Value, Mode=TwoWay}" FontFamily="Consolas" VerticalAlignment="Center" Margin="0,0,10,0" IsEnabled="{Binding IsNotProtected}"/>
                                    <TextBlock Grid.Column="3" Text="{Binding OriginalValue}" FontFamily="Consolas" FontSize="12" Opacity="0.7" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <CheckBox Grid.Column="4" IsChecked="{Binding IsIncrementOnly}" IsEnabled="False" Content="Increment Only" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBlock Grid.Column="5" Text="{Binding Permission}" FontFamily="Consolas" FontSize="12" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.Header>
                            <Grid Margin="5" Padding="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="1" Text="Name" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="2" Text="Value" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="3" Text="Original" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="4" Text="Increment Only" FontWeight="Bold" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="5" Text="Permission" FontWeight="Bold"/>
                            </Grid>
                        </ListView.Header>
                    </ListView>
                </Grid>
            </TabViewItem>
        </TabView>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" x:Name="StatusLabel" Text="Ready" VerticalAlignment="Center" Margin="10,0"/>
            <TextBlock Grid.Column="1" x:Name="DebugModeLabel" Text="" VerticalAlignment="Center" Margin="10,0"
                       FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
            <TextBlock Grid.Column="2" x:Name="TimerStatusLabel" Text="*" VerticalAlignment="Center" Margin="10,0" FontFamily="Consolas"/>
            <TextBlock Grid.Column="3" x:Name="CurrentTimeLabel" Text="" VerticalAlignment="Center" Margin="10,0" FontFamily="Consolas"/>
            <Button Grid.Column="4" x:Name="ClearLogButton" Content="Clear Log" Height="24" Margin="5,0" Click="OnClearLog"/>
            <ProgressRing Grid.Column="5" x:Name="LoadingRing" IsActive="False" Width="20" Height="20" Margin="10,0"/>
        </Grid>
    </Grid>
</Window>